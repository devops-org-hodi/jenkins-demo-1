pipeline{
    agent any
    stages{
        stage("build docker image"){
            steps{
                echo '========= building docker image ========='
                sh 'docker build -t app-1:v1 .'
                sh "docker images | grep -i app-1"

            }
        }
    }
}